{
  "name": "agent-model-router",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "amr": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "setup": "npm run build && node dist/cli.js install",
    "doctor": "npm run build && node dist/cli.js doctor",
    "disable": "npm run build && node dist/cli.js uninstall",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/*.test.js claude-mod/tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0"
  }
}
